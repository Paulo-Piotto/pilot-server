const Config = {
    rolesLevel: {// Represents the hierarchy of roles. The greater number is the greater role, and so on
        new: 0,
        basic: 1,
        admin: 2,
        root: 3,
    },
}

export default Config;